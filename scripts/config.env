# Shared configuration for all minecraft-server scripts
# Sourced by: 01-system-setup.sh, 02-deploy.sh, 03-apply-configs.sh,
#             04-pregen.sh, backup.sh
#
# Override the base install path by setting MINECRAFT_DIR before any script:
#   MINECRAFT_DIR=/data/minecraft sudo bash scripts/01-system-setup.sh

MINECRAFT_BASE="${MINECRAFT_DIR:-/opt/minecraft}"
DATA_DIR="${MINECRAFT_BASE}/data"
BACKUP_DIR="${MINECRAFT_BASE}/backups"
QUADLET_DIR=/etc/containers/systemd
CONTAINER_NAME=minecraft
SCRIPTS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_DIR="$(dirname "$SCRIPTS_DIR")"
